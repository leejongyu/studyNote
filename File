/*
  
  File Class ?
  
  입출력 스트림을 사용하면, 파일을 통한 입출력 작업을 할 수 있음.
  단, 파일의 제거나 디렉터리에 관한 작업은 입출력스트림으로 할 수 없다.
  
  => Java는 입출력 작업 이외의 파일, 디렉터리에 관한 작업을 File 클래스를 통해 처리한다.
 
*/

// File class의 메서드

// 해당 파일이 읽을 수 있는 파일인지를 검사함.
boolean canRead()	
// 해당 파일이 쓸 수 있는 파일인지를 검사함.
boolean canWrite()	
// 해당 파일 또는 디렉터리를 삭제함.
boolean delete()	
// 해당 파일이 존재하는지를 검사함.
boolean exists()	
// 해당 파일의 경로명을 문자열로 반환함.
String getPath()	
// 해당 파일의 경로명이 절대 경로인지를 검사함.
boolean isAbsolute()	
// 해당 파일이 디렉터리인지를 검사함.
boolean isDirectory()	
// 해당 파일이 파일인지를 검사함.
boolean isFile()	
// 해당 파일의 크기를 반환함.
long length()	
// 지정된 경로에 디렉터리를 생성함.
boolean mkdir()	
// 지정된 경로에 디렉터리를 생성하며, 필요한 모든 상위 디렉터리도 생성함.
boolean mkdirs()	
// 해당 파일의 이름을 전달된 파일 이름으로 변경함.
boolean renameTo(File dest)	

// 해당 파일의 속성을 변경함.
boolean setExecutable(boolean executable)
boolean setReadable(boolean readable)
boolean setWritable(boolean writable)
boolean setReadOnly()
