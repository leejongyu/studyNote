좌표값으로 방향을 구하는 문제에서 사용될 수 있는 코드

# 동, 북, 서, 남
int[] dx = [0, -1, 0, 1]
int[] dy = [1, 0, -1, 0]

#현재 위치
x, y = 2, 2

for i in range(5):
    # 다음 위치
    int nx = x + dx[i];
    int ny = y + dy[i];
    sysout(nx + " , " + ny);
 
